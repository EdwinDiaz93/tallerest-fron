export enum PublicRoutes {
    LOGIN = 'login',
}

export enum PrivateRoutes {
    PRIVATE = 'private',
    DASHBOARD = 'dashboard',
    MOVIES = 'peliculas',
    Rent = 'rentar',
    OPTIONS = 'opciones',
}