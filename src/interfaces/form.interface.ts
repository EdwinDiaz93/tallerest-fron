
// Login Values

export interface LoginFormValues {
    email: string;
    password: string;
}

export interface MovieFormValues {
    id?:number
    name: string;
    budget: number;
    date: string;
    duration: number;
}